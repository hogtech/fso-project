{
    "name": "backend",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "start": "node index.js",
        "dev": "nodemon index.js",
        "test": "echo \"Error: no test specified\" && exit 1",
        "build:ui": "rmdir /s /q build && cd ../frontend/ && npm run build && copy -r build ../backend/",
        "deploy": "git push heroku main",
        "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push && npm run deploy",
        "logs:prod": "heroku logs --tail"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "bcrypt": "^5.0.1",
        "cors": "^2.8.5",
        "dotenv": "^14.3.0",
        "express": "^4.17.2",
        "express-async-errors": "^3.1.1",
        "jest": "^27.4.7",
        "jsonwebtoken": "^8.5.1",
        "lodash": "^4.17.21",
        "mongoose": "^6.1.8"
    },
    "devDependencies": {
        "cross-env": "^7.0.3",
        "eslint": "^8.21.0",
        "nodemon": "^2.0.15",
        "supertest": "^6.2.2"
    }
}